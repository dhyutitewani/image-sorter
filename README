Image Sorting

Script to organize images into folders according to the device they were captured with.
Adds all the videos to a folder called videos.


set up:

# clone repo
$ git clone https://github.com/dhyutitewani/img-sorting.git

# cd into folder
$ cd img-sorting

# create virtual env and activate
$ virtualenv venv
$ source venv/bin/activate

# install requirements
$ pip install -r requirements.txt

# run script
$ cd src
$ python3 imgsort

